artifacts/
.DS_Store
qa-suggestions/

# dependencias instaladas: nunca al repo (cada proyecto las resuelve por su lockfile)
node_modules/
vendor/

# secretos: la API key de DeepSeek va acá, nunca al repo
.env

# specs adversariales generados por el LLM (Capa 2): artefactos, no fuente. Zona de cuarentena.
tests/e2e/generated/
